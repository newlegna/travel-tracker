# Neon Postgres
DATABASE_URL=

# Single-user gate: required in production.
# After changing the password in /settings, the hashed password is stored in the settings table.
APP_PASSWORD=

# Secret for signing session cookies (use at least 32 random chars).
# Defaults to APP_PASSWORD in development if omitted.
SESSION_SECRET=
