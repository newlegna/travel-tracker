# Neon Postgres
DATABASE_URL=

# Single-user gate: set in production (see middleware)
APP_PASSWORD=
# Optional: secret for signing session cookie (min 32 chars); defaults to APP_PASSWORD in dev
SESSION_SECRET=
